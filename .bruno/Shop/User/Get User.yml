info:
  name: Get User
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/users/{{user_example}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
